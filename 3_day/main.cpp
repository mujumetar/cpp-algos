#include <iostream>
using namespace std;

int main()
{
    // int mark;

    // cout << "Enter your marks: ";
    // cin >> mark;

    // string result = (mark >= 28) ? "passed" : "failed";

    // cout << "You " << result << " the exam.";

    int a = 10;
    int b = 10;
    int c = 10;

    (a > b) ? "a is greater" : "b is greater than a" ;
    (b > c) ? "b is greater" : "c is greater than b" ;
    (a > c) ? "a is greater" : "c is greater than a" ;
}